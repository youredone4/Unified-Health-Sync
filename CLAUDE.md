# Workflow preferences

- Each new request gets its own branch and its own pull request. Do not stack
  unrelated changes on an existing feature branch, and do not tack a new
  feature onto a branch whose PR is still open. Branch off `main`, commit the
  change, push, and open a new draft PR.
